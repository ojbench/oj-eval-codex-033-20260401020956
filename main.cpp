#include <bits/stdc++.h>
#include "src.hpp"

// The OJ expects an executable named `code`.
// For this problem, there is no specific I/O; we provide
// a minimal main that performs no I/O. The grader links
// against our list implementation via its own tests.
int main() {
    return 0;
}

